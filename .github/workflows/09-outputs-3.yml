name: outputs-3
on: workflow_dispatch
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: step-1
        uses: actions/checkout@v2
      - name: step-2
        run: echo "${{ toJson(steps)}}"
